coverage:
  precision: 2
  round: down
  range: 25...100
  # Specify files or directories to ignore
ignore:
- "internal/usecase/devices/wsman/*"
- "redfish/internal/controller/http/v1/generated/*"  # Exclude generated code (oapi-codegen)